<section class="movie-details">
  <div class="image">
    <%= image_tag @movie.image_file_name %>

    <% if current_user %>
      <div class="faves">
        <%= fave_or_unfave_button(@movie, @favourite)%>
        
        <div class="count">
          <%= @fans.size %>
        </div>
      </div>
    <% end %>
  </div>

  <div class="details">
      <h1> <%= @movie.title %> </h1>
  <h2>
      <%= @movie.released_on %> | <%= @movie.rating %>
  </h2>

  <p>
      <%= @movie.duration %> &bull; <%= @movie.description %>
  </p>

  <div class="reviews">
      <%= render "shared/stars", percent: @movie.average_stars_as_percent %>

      <%= link_to pluralize(@movie.reviews.size, "review"), movie_reviews_path(@movie) %>
  </div>

  <table>
      <tr>
          <th>Total Gross: </th>
          <td> <%= total_gross(@movie) %> </td>
      </tr>
  </table>

  <%= link_to "Write Review", new_movie_review_path(@movie), class: "review" %>
  
    <aside>
        <% if @fans.present? %>
            <h4>Fans</h4>
            <ul>
            <% @fans.each do |user| %>
                <li>
                <%= user.name %>
                </li>
            <% end %>
            </ul>
        <% end %>

        <% if @genres.present? %>
          <h4>Genres</h4>
          <ul>
            <% @genres.each do |g| %>
              <li><%= g.name %></li>
            <% end %>
          </ul>
        <% end %>
    </aside>
  </div>
</section>

<% if current_user_admin? %>
  <section class="admin">
    <%= link_to "Edit", edit_movie_path(@movie), class: "button" %>
    <%= link_to "Delete", @movie, class: "button", method: :delete,
      data: { confirm: "Are you sure you want to delete movie!?" } %>
  </section>
<% end %>


<%# <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img class="d-block w-100" src="avengers-end-game.png" alt="First slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src="avengers-end-game.png/800x400?auto=yes&bg=666&fg=444&text=Second slide" alt="Second slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src="avengers-end-game.png/800x400?auto=yes&bg=555&fg=333&text=Third slide" alt="Third slide">
    </div>
  </div>
</div> %>